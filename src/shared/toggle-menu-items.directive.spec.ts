import { ToggleMenuItemsDirective } from './toggle-menu-items.directive';

describe('ToggleMenuItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleMenuItemsDirective();
    expect(directive).toBeTruthy();
  });
});
